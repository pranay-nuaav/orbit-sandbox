# calc — Orbit's sandbox repository

A deliberately small project with a test suite that passes today, so Orbit has
somewhere real to branch, edit, run tests and open a PR against.

Synthetic. No client data, ever.

    python -m pytest -q
